<UserControl x:Class="PersonalBudgetPlanner.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalBudgetPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" CornerRadius="10" Padding="30" Margin="20">
        <StackPanel MaxWidth="400" HorizontalAlignment="Left">
            <TextBlock Text="Användarprofil &amp; Inställningar" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <Label Content="Årsinkomst (Brutto)"/>
            <TextBox Text="{Binding UserProfile.AnnualIncome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

            <Label Content="Arbetstimmar per år (Standard 1920)"/>
            <TextBox Text="{Binding UserProfile.AnnualWorkHours, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Spara Inställningar" 
                        Command="{Binding SaveProfileCommand}" 
                        Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Width="140"/>

                <Button Content="Tillbaka till Budget" 
                        Command="{Binding BackCommand}" 
                        Style="{StaticResource PrimaryButtonStyle}" Background="Gray" Width="140"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>